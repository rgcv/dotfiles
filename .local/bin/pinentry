#!/bin/sh

[ -n "$WSL_DISTRO_NAME" ] && exec ~/.local/bin/pinentry-wsl-ps1.sh "$@"
[ -e /usr/lib/libgcr-base-3.so.1 ]  && exec pinentry-gnome3 "$@"
[ -e /usr/lib/libgtk-x11-2.0.so.0 ] && exec pinentry-gtk-2  "$@"
exec pinentry-curses "$@"
